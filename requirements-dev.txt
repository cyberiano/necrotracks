# Para correr los tests en la Mac: python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
numpy~=2.5
soundfile~=0.14
soxr~=1.1
pytest
mido
# web: tests (TestClient usa httpx) y probarla en la Mac con `python -m web`
fastapi
uvicorn
httpx
